apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: nginx-ingress-app
spec:
  releaseName: nginx-ingress1
  chart:
    spec:
      chart: ingress-nginx
      sourceRef:
        kind: HelmRepository
        name: ingress-nginx
        namespace: flux-system
      version: "3.23.0"
  interval: 30m
  install:
    remediation:
      retries: 3
  values:
    controller:
    kind: Deployment
